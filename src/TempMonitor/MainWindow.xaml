<Window x:Class="TempMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TempMonitor"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        SizeToContent="WidthAndHeight" ShowActivated="False"
        MouseLeftButtonDown="OnDrag">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#E8E8E8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="Label" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Foreground" Value="#9A9A9A"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="Value" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="TextAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <Border Background="#CC101418" CornerRadius="8" Padding="10,6"
            BorderBrush="#33FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="46"/>
                <ColumnDefinition Width="46"/>
                <ColumnDefinition Width="52"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource Label}" Text="CPU"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource Value}" x:Name="CpuTempText" Text="--°"/>
            <TextBlock Grid.Row="0" Grid.Column="2" Style="{StaticResource Value}" x:Name="CpuLoadText" Text="--%"/>
            <TextBlock Grid.Row="0" Grid.Column="3" Style="{StaticResource Value}" x:Name="CpuPowerText" Text="--W"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource Label}" Text="GPU"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Style="{StaticResource Value}" x:Name="GpuTempText" Text="--°"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Style="{StaticResource Value}" x:Name="GpuLoadText" Text="--%"/>
            <TextBlock Grid.Row="1" Grid.Column="3" Style="{StaticResource Value}" x:Name="GpuPowerText" Text="--W"/>
        </Grid>
    </Border>
</Window>
